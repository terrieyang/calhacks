Inventory Name: <%= @inventory.name %><br />
<%= link_to 'All Inventories', inventories_path%>
<%= link_to 'Edit', edit_inventory_path(@inventory), class: 'button' %>
<%= link_to 'Delete', @inventory, 
                  class: 'button',
                  method: :delete, data: { confirm: 'Are you sure?' } %>